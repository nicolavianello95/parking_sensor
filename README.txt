Organization of the folders:

final_report: contains the final report of the whole project in PDF format.

firmware: contains all the source code for the microcontroller.

PC_driver: contains the python script that allows the PC to interface with the FPGA.

VHDL: contains all the RTL deisgn to load inside of the FPGA.